<!DOCTYPE html>
<html>
<head>
  <title>SocialAuthentication</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class = "container">
    <div class = "row">
      <% if current_user %>
        <%= link_to "Sign out", signout_path, id: "sign_out", class: "btn btn-block btn-primary" %>
            Signed in as <strong><%= current_user.name %></strong>!
      <% else %>
        <div class = "row">
          <div class = "col-md-6">
            <%= link_to "Sign in with Facebook", "/auth/facebook", id: "sign_in", class: "btn btn-large btn-block btn-primary" %>
          </div>
          <div class = "col-md-6">
            <%= link_to "Sign in with google", "/auth/google_oauth2", id: "sign_in", class: "btn btn-large btn-block btn-danger" %>
          </div>

        </div>
      <% end %>
    </div>
    <%= yield %>
  </div>

</body>
</html>
